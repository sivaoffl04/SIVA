var typedInstance = new Typed(".text", {
    strings:["Frontend Developer", "Backend Developer", "Full Stack Developer"],
    typeSpeed: 60,
    backSpeed: 60,
    backDelay: 1000,
    loop: true
});